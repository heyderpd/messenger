{
  "name": "eight-queens",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "docs": "rm -rf docs && mkdir docs && cp -a ./build/. ./docs/",
    "build": "PUBLIC_URL=. react-scripts build && npm run docs"
  }
}
